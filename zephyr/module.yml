# Delete all unused properties.

# Give your module a name
name: zmk-keyboards-keyball
build:
  # List your dependencies
  depends:
    - zmk-pmw3610-driver
  # Folder containing a "CMakeLists.txt" (. = root)
  cmake: .
  # Kconfig filepath
  kconfig: Kconfig
  settings:
    board_root: .
    dts_root: .
